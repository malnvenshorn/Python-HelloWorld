name: Create version tag

on:
  push:
    branches:
      - master

jobs:
  create-tag:
    name: Create tag
    uses: malnvenshorn/Python-HelloWorld/.github/workflows/create-git-tag-from-python-packages.yaml@master
